# Deployment resource profile

Generated by `pnpm metrics:deployment` — do not edit by hand.

- Commit: `782b5ffcf17a5231833368a7478f52f2d6de4c02`
- Generated: 2026-08-12T16:24:02.473Z
- Measured on the host below, not in CI. These figures characterise a machine,
  so a shared runner would produce a number with no deployment meaning.

## Environment

| Property | Value |
| --- | --- |
| commit | 782b5ffcf17a5231833368a7478f52f2d6de4c02 |
| node | v22.19.0 |
| platform | Windows_NT 10.0.22631 (x64) |
| hostCpu | Intel(R) Xeon(R) w5-2555X |
| hostLogicalCores | 28 |
| hostTotalMemoryGiB | 63.25 |
| dockerVersion | 29.6.1 |
| dockerOperatingSystem | Docker Desktop |
| dockerKernel | 6.18.33.2-microsoft-standard-WSL2 |
| dockerCgroupVersion | 2 |
| dockerNCPU | 28 |
| dockerMemTotalGiB | 30.97 |
| dockerBackend | Docker Desktop, WSL2 backend |
| settleSeconds | 30 |
| idleSampling | polled, 12 samples at 5000 ms |
| loadSampling | streamed from one `docker stats` process, ~1 reading/s, for the benchmark’s duration |
| wslConfig | absent — WSL2 defaults apply (memory ≈ 50% of host RAM, all logical processors) |

## Resource windows

Memory and CPU are per-container readings from `docker stats`, reported as a
distribution across repeated samples rather than as a single reading. A lone
`docker stats --no-stream` CPU value is unreliable, and a one-off idle sample is
what produced the unsupportable "CPU utilization < 0.5%" figure this replaces.

**These totals are not the host cost.** `docker stats` reports each container’s
cgroup working set and excludes the overhead of the virtual machine the engine
runs in — on Docker Desktop with the WSL2 backend, the VM itself. Treat the
stack total as a lower bound on what a host must provide.

### Idle, single-node

12 samples at 5000 ms — Single-node (docker-compose.yml).

| Container | Memory p50 | Memory max | CPU p50 | CPU max |
| --- | ---: | ---: | ---: | ---: |
| `lis-profile-backend` | 61.22 MB | 62.84 MB | 0.03% | 16.83% |
| `lis-profile-frontend` | 22.87 MB | 23.11 MB | 0% | 0% |
| `lis-profile-postgres` | 42.48 MB | 43.27 MB | 0.02% | 2.37% |
| **Stack total** | **126.55 MB** | **128.92 MB** | **0.06%** | **16.85%** |

### Under load (pnpm bench)

66 samples at 1000 ms — Single-node (docker-compose.yml).

| Container | Memory p50 | Memory max | CPU p50 | CPU max |
| --- | ---: | ---: | ---: | ---: |
| `lis-profile-backend` | 204.40 MB | 267.90 MB | 262.96% | 297.24% |
| `lis-profile-frontend` | 22.80 MB | 22.81 MB | 0% | 0% |
| `lis-profile-postgres` | 113.00 MB | 113.40 MB | 0.09% | 43.52% |
| **Stack total** | **340.10 MB** | **404.10 MB** | **264.69%** | **300.59%** |

### Idle, replicated (primary + hot standby)

12 samples at 5000 ms — Replicated primary + hot standby (docker-compose.prod.yml).

| Container | Memory p50 | Memory max | CPU p50 | CPU max |
| --- | ---: | ---: | ---: | ---: |
| `lis-profile-repl-backend` | 52.27 MB | 53.23 MB | 0% | 18.59% |
| `lis-profile-repl-frontend` | 22.72 MB | 22.82 MB | 0% | 0% |
| `lis-profile-repl-postgres-primary` | 80.25 MB | 80.63 MB | 0.02% | 4.41% |
| `lis-profile-repl-postgres-standby` | 29.30 MB | 29.57 MB | 0.02% | 4.32% |
| **Stack total** | **184.60 MB** | **184.86 MB** | **0.06%** | **18.63%** |

## Storage

Measured by the paired benchmark run on the pristine seeded corpus, before it
inserted cases of its own. Full per-table breakdown in `benchmark-2026-08-12T16-24-02-473Z.md`.

| Measure | Value |
| --- | ---: |
| Cases in the corpus | 300 |
| Case-scaling tables, total | 1.73 MB |
| Reference and configuration tables, total | 0.96 MB |
| **Storage per case** | **5.9 KB** |

## Reproducing this

```bash
pnpm metrics:deployment
```

The profile stack runs under its own Compose project name with its own volumes and
remapped ports, so it neither reads nor destroys a developer’s database and can run
while the default stack is up. It starts from `down -v`, so the corpus is always the
deterministic 300-case seed.
