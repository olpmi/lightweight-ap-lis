# Implemented system characteristics

Generated by `pnpm metrics:system` — do not edit by hand.

- Commit: `782b5ffcf17a5231833368a7478f52f2d6de4c02`
- Generated: 2026-08-11T20:35:15.806Z

This table is derived from repository content at the commit above. It corresponds to
Table 2 of the manuscript, excluding the idle deployment footprint rows, which are
host measurements rather than repository facts.

| Characteristic | Value | Counting rule |
| --- | ---: | --- |
| **Structured reporting content** | | |
| Gross examination templates, malignant resection specimens | 9 | Count of `*.core.json` files under packages/shared/src/templates/assets (language overlay files excluded). Gross = path contains a `/gross/` segment. |
| Microscopic reporting templates, malignant (CAP-aligned) | 19 | Count of `*.core.json` files under packages/shared/src/templates/assets (language overlay files excluded). Microscopic = non-gross, family does not end in `_cytology`. |
| Cytology reporting templates (Bethesda, Milan, Paris, Yokohama) | 7 | Count of `*.core.json` files under packages/shared/src/templates/assets (language overlay files excluded). Cytology = non-gross, family ends in `_cytology`. |
| Total structured reporting templates | 35 | Count of `*.core.json` files under packages/shared/src/templates/assets (language overlay files excluded). |
| **Ancillary testing catalog** | | |
| Ancillary test categories | 5 | Distinct categories present in the seeded catalog (HE_LEVELS, IHC, MOLECULAR, SEND_OUT, SPECIAL_STAIN). The AncillaryCategory enum additionally defines HE, which is applied to per-block H&E orders created at accessioning rather than ordered from the catalog. |
| Individual immunohistochemistry orderables | 27 | Rows in the `ancillary_orderable` INSERT of prisma/migrations/20260609000000_seed_reference_data/migration.sql. Category = IHC. |
| Immunohistochemistry panels | 9 | Rows in the `ancillary_panel` INSERT of the same migration. Category = IHC. |
| Molecular panels (colorectal, lung, PD-L1) | 3 | Rows in the `ancillary_panel` INSERT of the same migration. Category = MOLECULAR. |
| Special stains | 5 | Rows in the `ancillary_orderable` INSERT of prisma/migrations/20260609000000_seed_reference_data/migration.sql. Category = SPECIAL_STAIN. |
| Send-out categories | 3 | Rows in the `ancillary_orderable` INSERT of prisma/migrations/20260609000000_seed_reference_data/migration.sql. Category = SEND_OUT. |
| Total preconfigured panels | 12 | Rows in the `ancillary_panel` INSERT of the same migration. |
| **Data model** | | |
| Relational entities in the data model | 19 | `model` blocks in prisma/schema.prisma (22) excluding 3 structural tables: OrderSequenceYear (identifier-sequence counter); CustomTemplateTranslation (translation side-table); AncillaryPanelItem (pure join table (panel ↔ orderable)). Enums are not counted as entities. |
| Total database tables | 22 | All `model` blocks in prisma/schema.prisma, including join, sequence, and translation tables. |
| Enumerated types | 7 | All `enum` blocks in prisma/schema.prisma. |
| **Data model and demonstration data** | | |
| Synthetic demonstration cases | 300 | `ORDER_COUNT` in prisma/seed.ts. Run `pnpm db:characterize` against a seeded database for the realized case mix and stage distribution. |
| Pseudo-random generator seed | 42 | Seed passed to `makeRng` in prisma/seed.ts. Fixing this value is what makes the synthetic corpus byte-reproducible. |
